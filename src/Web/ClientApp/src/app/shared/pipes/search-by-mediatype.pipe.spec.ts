import { SearchByMediatypePipe } from './search-by-mediatype.pipe';

describe('SearchByMediatypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByMediatypePipe();
    expect(pipe).toBeTruthy();
  });
});
